This project includes three parts: preprocess pov data, generate training data, and mention selection.

The usage of each component is described in the corresponding folder.

The order to use each component is as follows: preprocess pov data, generate training data, mention selection.

The annotated pov data is in the folder "pov_data".